[package]
name = "jamodio-au-host"
version = "0.5.4-17"
edition = "2021"
license = "LicenseRef-Jamodio-Proprietary"
publish = false
description = "Jamodio AudioUnit (AU) plugin host â€” macOS uniquement, impl du trait PluginHost de jamodio-audio-core"

# Phase 2 Windows : ce crate restera macOS-only (frameworks Apple obligatoires).
# Un crate frÃ¨re `jamodio-vst3-host` couvrira Windows. Trait commun = jamodio-audio-core::plugin_host.

[dependencies]
jamodio-audio-core = { path = "../jamodio-audio-core" }
tracing = "0.1"

[build-dependencies]
cc = "1.0"
